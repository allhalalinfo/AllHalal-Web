# Промпт для отправки Backend агенту

Скопируй текст ниже и отправь своему Cursor агенту, подключенному к Hetzner:

---

Привет! У меня для тебя задание по настройке автоматического обновления новостей (RSS feeds → Redis → FastAPI). Все бесплатно, используем только существующие ресурсы сервера.

**Прочитай детальные инструкции:**
`/Users/adelyanurusheva/Desktop/Allhalal-Web/docs/CURSOR-AGENT-INSTRUCTIONS.md`

**Выполни 3 задачи последовательно:**

1. **Создать RSS parser скрипт** `/root/allhalal_news_updater.py`
   - Установи: `pip3 install feedparser redis requests`
   - Код готов в документации (копируй как есть)

2. **Добавить FastAPI endpoint** `/api/v1/news/cached`
   - Найди где лежит FastAPI проект: `find /root /home /opt -name "main.py" -o -name "app.py" | xargs grep -l "FastAPI"`
   - Добавь новый роут (код готов в документации)

3. **Настроить Cron Job**
   - Расписание: `*/30 * * * *` (каждые 30 минут)
   - Команда и настройка логов - в документации

**ВАЖНО:** Используй ТОЛЬКО бесплатные решения. НЕ устанавливай Upstash или другие внешние сервисы.

**После завершения напиши отчет:**
- Результаты тестов (команды в документации)
- Что работает, что нет
- Любые проблемы

Начинай с чтения `/docs/CURSOR-AGENT-INSTRUCTIONS.md` - там весь код и все команды готовы.

---

**Время:** ~15-20 минут  
**Стоимость:** $0
