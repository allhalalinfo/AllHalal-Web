/**
 * Zakat & finance guides — static RSC, minimal client JS.
 */
export default function GuidesLayout({ children }: { children: React.ReactNode }) {
  return <div className="guides-scope">{children}</div>;
}
